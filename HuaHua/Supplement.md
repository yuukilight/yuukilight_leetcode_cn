# unordered_map
简单的使用
```c++
#include <unordered_map>
using namespace std;
// 创建
unordered_map<int, int> indices;
// 基本函数
indices.count(end);
indices.begin();
indices.end();
indices.find(key);
indices.empty();
indices.clear();

erase;
emplace;
```